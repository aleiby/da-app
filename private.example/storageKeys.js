// NFT.storage API key for uploading card images to IPFS
// Copy this file to ../private/storageKeys.js and update with your key

module.exports = {
    default: {
        // NFT.storage API key (starts with "eyJ...")
        //
        // To get an API key:
        // 1. Visit https://nft.storage/
        // 2. Sign up for a free account
        // 3. Go to "API Keys" in your account dashboard
        // 4. Click "New Key" and copy the generated key
        //
        // This key is used to upload card images to IPFS when minting new card sets.
        apiKey: "eyJ..."
    }
};
